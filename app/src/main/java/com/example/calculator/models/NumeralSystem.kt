package com.example.calculator.models

/** A number can either be represented in the decimal system or in the binary system.
 * e.g. 35 or 0b101001
 */
enum class NumeralSystem {
    DECIMAL,
    BINARY
}